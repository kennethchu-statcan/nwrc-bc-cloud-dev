# Install Dependencies
```R
> install.packages(pkgs = c("cowplot","dplyr","fda","ggplot2","logger","R6", "knitr", "tidyr"))

# install the feature engine
> install.packages( repos = NULL, type = "source", pkgs = "fpcFeatures_0.0.0.0001.tar.gz" ) 
```
